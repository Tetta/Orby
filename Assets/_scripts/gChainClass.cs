using UnityEngine;
using System.Collections;

public class gChainClass : MonoBehaviour {


	// Use this for initialization w
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

	void OnCollisionEnter2D(Collision2D collisionObject){
		//Debug.Log("OnCollisionEnter2D " + collisionObject.gameObject.name);
		//if (collisionObject.gameObject.name == "orby") {
			//staticClass.instanceStaticClass.webState = "collisionOrby";
			//Debug.Log (Time.time + ": webState = collisionOrby");
		//}
		
		
		
	}
	void OnMouseDown () {
		//Debug.Log("OnMouseDown chain");

	}
}
